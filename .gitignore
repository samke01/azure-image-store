.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
set-env.ps1
!set-env.example.ps1

# Agent VM SSH keypair (private key must never be committed)
clouddevops-agent
clouddevops-agent.pub

# Python / Flask app
__pycache__/
*.pyc
.venv/
deploy.zip
